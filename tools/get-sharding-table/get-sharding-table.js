if (!process.argv[2]) {
    console.log('Missing argument BLOCKCHAIN');
    console.log('Usage: npm run get-sharding-table (testnet, mainnet)');
    process.exit(1);
}

(async () => {
    // initialize blockchain manager and sharding table
})();
